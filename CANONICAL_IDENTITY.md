# Canonical Identity

## Canonical names

- `CeloHT` is the active project.
- `Celo-HaiTi` is the canonical GitHub organization.
- `Celo-HT` is obsolete and prohibited in the current editable corpus.

## Canonical currency

- `USDm` is the current canonical currency terminology.
- `cUSD` is obsolete and prohibited in the current editable corpus.

These declarations are enforcement metadata. The vocabulary audit excludes this
file and `GLOBAL_REFERENCE_AUDIT.md` from the content scan because those files
must name the prohibited patterns explicitly.